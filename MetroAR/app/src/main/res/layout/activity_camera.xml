<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000">

    <!-- Preview da câmera -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >
    </androidx.camera.view.PreviewView>

    <!-- Botão Flash (topo esquerdo) -->
    <ImageButton
        android:id="@+id/btnFlash"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_flash_on"
        app:tint="@android:color/white"
        android:contentDescription="@string/flash_toggle" />


    <!-- Botão Captura (centro inferior) -->
    <ImageButton
        android:id="@+id/btnCapture"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="40dp"
        android:background="@drawable/bg_capture_button"
        android:contentDescription="@string/capture_button" />

    <!-- Botão Home (canto inferior esquerdo) -->
    <ImageButton
        android:id="@+id/btnHome"
        android:layout_width="55dp"
        android:layout_height="55dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="25dp"
        android:layout_marginBottom="45dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_back"
        app:tint="@android:color/white"
        android:contentDescription="@string/go_home" />

    <!-- Botão Galeria (canto inferior direito) -->

    <!-- Botão AR (acima da galeria) -->
    <ImageButton
        android:id="@+id/btnAR"
        android:layout_width="55dp"
        android:layout_height="55dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="25dp"
        android:layout_marginBottom="45dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_ar"
        app:tint="@android:color/white"
        android:contentDescription="@string/ar_mode" />

</FrameLayout>